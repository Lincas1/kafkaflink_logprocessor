package KafkaProducer;

import java.util.List;

public class Test {

    /*
    public static void main(String[] args) {


        List<KafkaRecord> res = FileScan.scanByFileName("displaypolicyd.log");

        for (KafkaRecord each : res) {
            System.out.println(each.getKeyAsString());
            System.out.println(each.getValueAsString());
        }

        String path = "displaypolicyd.log";
        String topic = "testtopic1";

        LogProducer logs = new LogProducer().setBatchRecord(path).setTopic(topic);
        logs.batchPushLog();

    }

    */
}
